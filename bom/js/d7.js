/*
  BOM [Browser Object Model]
  - stop() //stop loading
  - print() //to print
  - focus() //docus work as focus in input
  - scrollTo(x, y || Options)
  - scroll(x, y || Options)
  - scrollBy(x, y || Options) like scrollTo but it work many time as we call it 
  if we call it 2 times it will work 2 times but ScrollTo even if we type it 
  many times it will work once
*/

// let myNewWindow = window.open("https://google.com", "", "width=500,height=500");

// window.scrollTo({
//   left: 500,
//   top: 200,
//   behavior: "smooth"
// });